{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev -- --hostname 127.0.0.1",
  "installCommand": "npm install",
  "outputDirectory": ".vinext",
  "functions": {
    "app/api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "env": {
    "NEON_BRANCH": "production"
  }
}
